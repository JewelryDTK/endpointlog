# EndpointLog

Static knowledge site for practical Microsoft workplace guides, builds and insights.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The static site is written to `dist/client/`. Pushes to `main` deploy through GitHub Pages after the repository and Pages source are configured.

import {ArrowUpRight,ArrowRight} from 'lucide-react';
import {posts,topics,articleUrl,type Post} from '@/lib/content';
export function MagazineRail(){return <aside className="mag-rail"><div className="rail-caption">THE NOTEBOOK</div><a className="rail-all" href="/blog">All entries <span>{String(posts.length).padStart(2,'0')}</span></a><nav className="rail-topics" aria-label="Browse topics">{topics.map(topic=><a key={topic} href={'/blog?topic='+encodeURIComponent(topic)}><span>{topic}</span><small>{posts.filter(p=>p.categories.includes(topic)).length}</small></a>)}</nav><a className="rail-tools" href="/tools">Scripts & tools <ArrowUpRight size={15}/></a><div className="rail-author"><img src="/assets/jewelry.jpg" width="52" height="52" alt="Jewelry Kenepa"/><strong>Jewelry Kenepa</strong><span>Senior Technical Consultant<br/>at Rapid Circle.</span><p>Notes from the workday. Shared with the community.</p><a href="/about">About me <ArrowRight size={14}/></a></div><a className="rail-follow" href="https://www.linkedin.com/in/jewelrykenepa/" target="_blank" rel="noopener noreferrer">Find me on LinkedIn ↗</a></aside>;}
export function EntryRow({post}:{post:Post}){return <article className="entry-row"><div className="row-body"><div className="row-meta"><span>{post.categories[0]}</span><time>{post.date}</time><span>{post.minutes} min read</span></div><h3><a href={articleUrl(post)}>{post.title}</a></h3><p>{post.summary}</p></div><a href={articleUrl(post)} className="row-thumbnail" aria-label={'Read '+post.title}><img src={post.image} alt="" width="150" height="106" loading="lazy"/></a><a href={articleUrl(post)} className="row-open" aria-label={'Read '+post.title}><ArrowUpRight size={20}/></a></article>;}
export function ToolShelf(){return <section className="tool-shelf"><div className="shelf-heading"><h2>From the toolbox</h2><span>02</span></div>{posts.filter(p=>[572,438].includes(p.id)).map(post=><a className="shelf-item" href={articleUrl(post)} key={post.id}><span className="shelf-type">PowerShell · Microsoft Graph</span><h3>{post.id===572?'Intune tenant cleanup':'Autopilot group tags'}</h3><span>Read the build notes <ArrowUpRight size={14}/></span></a>)}<a className="shelf-all" href="/tools">Open the toolbox <ArrowRight size={16}/></a></section>;}